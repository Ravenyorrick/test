# Apollo API key (required)
# Create at: https://docs.apollo.io/docs/create-api-key
APOLLO_API_KEY=

# Optional: public HTTPS webhook URL for waterfall email enrichment
# Apollo requires a valid HTTPS webhook for run_waterfall_email=true
APOLLO_WEBHOOK_URL=

# Optional: enable debug logging (never logs the API key)
DEBUG=false
