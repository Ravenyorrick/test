# Troubleshooting

## Phase 1

If the application does not launch:

1. Run `npm install`.
2. Run `npm run typecheck`.
3. Run `npm test`.
4. Run `npm run build`.
5. Run `npm run dev`.

Audio capture, voice conversion, and virtual microphone troubleshooting will be added as those native subsystems are implemented.
