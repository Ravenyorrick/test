# Windows Native Components

Windows virtual microphone driver and native integration code will live here.

Production driver work must use proper Windows virtual audio device architecture and signing.
