# Document viewer

Open `index.html` in a browser to show the loading screen.

To change the website opened after the loading screen, edit the `redirectUrl`
value in the `page-config` JSON block near the bottom of `index.html`.

The default loading delay is 15 seconds and can be changed with
`loadingDelayMs`.
